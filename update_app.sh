#!/bin/bash

# We will modify App.tsx to use AdaptiveFilter when adaptive mode is on.
# First, ensure TapWeightsPlot is available.
# Then update ControlRail to include an "Adaptive Mode" toggle instead of "Auto-Estimate Parameters".
# Then update App.tsx to run the adaptive filter.

